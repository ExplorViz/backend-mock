services:
  ## ExplorViz Backend Mock ##

  backend-mock:
    build: ./backend-mock
    networks:
      - explorviz
    ports:
      - 8083:8083
      - 8084:8084

networks:
  explorviz:
    name: explorviz
